services:
  - type: web
    name: signal-sieve
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --timeout 30 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
